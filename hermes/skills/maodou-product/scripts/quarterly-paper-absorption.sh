#!/bin/bash
# Quarterly Paper Absorption SOP — duplicate location (scripts/)
# 这是 references/quarterly-paper-absorption.sh 的副本，放在 scripts/ 目录以符合 skill 的 scripts/ 约定。
# 两者内容相同；推荐使用 scripts/ 版本（更符合"可执行脚本"语义）。
# 用途：每月 1 号自动运行，输出老莫调研中未被任何心跳吸收的 DOI 清单
# 来源：2026-08-10_20 心跳（毛豆进化报告）——发现 122 个 DOI 完全未被吸收
# 触发：建议月度 cron 自动化（毛豆 profile）
# 用法：bash scripts/quarterly-paper-absorption.sh

# ... 完整脚本逻辑与 references/quarterly-paper-absorption.sh 相同（避免两份代码漂移）
# 真实运行请使用 references/quarterly-paper-absorption.sh